<!DOCTYPE html>
<html lang="EN">
<head>
  <title>Заказы</title>
</head>

<body>
<div class="d-flex justify-content-center">
  <div class="col-md-3">
    <h4 class="fw-bolder mt-0 mb-4"><%= @role == "driver" ? "Доступные Заказы" : "История Заказов" %></h4>
    <%= render partial: "orders/partials/order_list", locals: { orders: @orders } %>
    <%= link_to "Обратно", :back %>
  </div>
</div>
</body>
</html>
